var wordOrder = function(sentence) {
  var origArray = sentence.split(" ");
  var manyWords = [];
  var count = [];

  for (var i = 0; i < origArray.length; i++) {
    if (origArray[i] manyWords[i]) {
        manyWords.push(origArray[i]);
    } else {

    }
  };
  return manyWords;
};
